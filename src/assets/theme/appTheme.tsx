import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  iconBackgorund: {
    width: 300,
    height: 300,
    position: 'absolute',
    opacity: 0.2,
    alignContent: 'center',
  },
  title: {
    fontSize: 30,
    fontWeight: 'bold',
  },
});
